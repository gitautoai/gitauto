name: Stage PyTest Workflow

on:
  push:
    branches:
      - 'stage*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Set up Python 3.12
      uses: actions/setup-python@v2
      with:
        python-version: 3.12

    - name: Install pytest
      run: |
        pip install pytest pytest-cov
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Set PYTHONPATH
      run: echo "PYTHONPATH=$GITHUB_WORKSPACE" >> $GITHUB_ENV

    - name: Run pytest
      env:
<<<<<<< HEAD:.github/workflows/nikita-test.yml
        SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.NIKITA_SUPABASE_SERVICE_ROLE_KEY }}
        SUPABASE_URL: ${{ secrets.NIKITA_SUPABASE_URL }}
      run: pytest --doctest-modules --junitxml=junit/test-results.xml --cov=com --cov-report=xml --cov-report=html
=======
        SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.STAGE_SUPABASE_SERVICE_ROLE_KEY }}
        SUPABASE_URL: ${{ secrets.STAGE_SUPABASE_URL }}
      run: pytest
>>>>>>> 2058a35f68f0040aa22f6b4e556a72431fa1d771:.github/workflows/stage-test.yml
